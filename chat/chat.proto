syntax = "proto3";

package chat;

option go_package = "chat/proto;proto";

message ChatMessage {
  string user = 1;
  string message = 2;
  int64 timestamp = 3;
}

service ChatService {
  rpc Chat(stream ChatMessage) returns (stream ChatMessage);
}
